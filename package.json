{
  "name": "aquabit-lab-site",
  "version": "1.0.0",
  "private": true,
  "description": "AquaBit LAB 公式サイト（静的HTML + three.js/WebGPU ヒーロー）",
  "scripts": {
    "build": "esbuild src/hero.js --bundle --format=esm --minify --target=es2022 --outfile=js/hero.min.js",
    "watch": "npm run build -- --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "three": "0.185.1"
  }
}
